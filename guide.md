为了让 Project Page 既能完美衬托这些图表，又能保持极简、高级的学术审美，以下是网页背景与整体风格的设计方案：

### 一、 网页背景颜色设计

为了确保包含复杂数据的图表（如散点图、小提琴图）成为视觉焦点，网页背景必须保持**低干扰和高通透感**。建议采用以下组合：

* **主背景色：纯白 (`#FFFFFF`) 或 极浅灰 (`#FAFAFA`)**
    * **策略：** 大面积使用纯白作为主视觉和内容区域的背景。这能最大限度地保证图表边缘的干净，让 `#BFDFD2` (浅青) 和 `#DBCB92` (浅卡其) 这种极浅的图表颜色也能清晰显现。
* **区块区分色：极淡的薄荷青 (`#F2F8F6`) 或 象牙白 (`#FCFBF6`)**
    * **策略：** 当你需要区分不同的网页区块（例如：Abstract 和 Method 部分）时，不要使用生硬的边框。可以从你的原配色中提取最浅的颜色，将其透明度降至 10% 左右，作为大区块的背景底色，形成温和的视觉分区。

### 二、 网页排版与风格建议

* **坚守 2D 扁平化风格**
    * 网页上的所有 UI 元素（按钮、卡片、图标）建议采用纯平设计。**严格避免任何渐变色、阴影（Drop shadow）或 3D 凸起效果。** 这种克制的“低饱和+扁平”风格能与你的科研图表完美契合。
* **利用“分割线”而非“网格”**
    * 在排列多张论文图表或文本块时，避免将它们框在沉重的网格或带边框的卡片中。建议留出充足的纯白留白（Whitespace），并仅在必要时使用极细、低对比度的**单色分割线**（如 `#E2E8F0`）来划分内容层次。
* **组件色彩映射**
    * **主标题与正文：** 放弃纯黑 (`#000000`)，改用深石板灰（例如 `#2C3E50`）。这不仅对眼睛更友好，也能维持整体低饱和度的优雅感。
    * **主干操作按钮（如 Paper, Code, Model 链接）：** 使用配色方案中的中坚力量——深青色 (`#51999F` 或 `#4198AC`)。
    * **强调与高亮（如重点指标、Badge）：** 使用配色右侧的暖橙色 (`#ED8D5A` 或 `#EA9E58`)。在冷色调主导的页面中，少量的暖橙色能够迅速抓住浏览者的注意力。

### 三、 字体搭配 (Typography)

* **无衬线字体主导：** 建议采用结构严谨、字重分明的无衬线字体（如 Inter, Roboto, 或 Helvetica Neue）。这种字体排版能赋予网页类似顶级学术会议（如 NeurIPS / ICLR）收录论文的现代感和专业度。
